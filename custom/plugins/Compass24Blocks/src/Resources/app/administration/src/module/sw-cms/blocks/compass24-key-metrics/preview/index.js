import template from './sw-cms-preview-compass24-key-metrics.html.twig';
import './sw-cms-preview-compass24-key-metrics.scss';

const { Component } = Shopware;

/**
 * Preview component shown in the CMS block selection
 */
Component.register('sw-cms-preview-compass24-key-metrics', {
    template
});
